---

import { ui } from '../i18n/ui';

const FORMSPREE_URL = "https://formspree.io/f/xqedpqpp"

const lang = Astro.currentLocale || 'es';
const t = ui[lang as keyof typeof ui];
---

<section id="contacto" class="contact-section">
    <div class="container">
    <h2 class="section-title">{t['contact.title']}</h2>
    <p class="contact-text">
        {t['contact.subtitle']}
    </p>

    <div class = "form-wrapper">

        <form action={FORMSPREE_URL} method="POST" class="contact-form">

            <div class="form-group">
                <label for = "name">{t['form.name']}</label>
                <input type="text" id="name" name="name" placeholder={t['form.name.example']} required>
            </div>

            <div class="form-group">
                <label for="email">{t['form.email']}</label>
                <input type="email" id="email" name="email" placeholder={t['form.email.example']} required>
            </div>

            <div class="form-group">
                <label for="message">{t['form.message']}</label>
                <textarea id="message" name="message" rows="5" placeholder={t['form.message.example']} required></textarea>
            </div>

            <input 
                type="text" 
                name="_gotcha" 
                style="display:none" 
                tabindex="-1" 
                autocomplete="off" 
            />

            <button type="submit" class="submit-btn">{t['form.submit']}</button>

        </form>
        
       
    </div>
    </div>
</section>

<style>
    .contact-section {
        padding: 1rem 1rem;        
        position: relative;
        scroll-margin-top: 100px;
    }

    .section-title {
        font-size: 2.5rem; 
        text-align: center;
        margin-bottom: 3rem;
        color: #333;
        position: relative;
        display: inline-block;
        left: 50%;
        transform: translateX(-50%);
        z-index: 1;
    }

    .section-title::after {
        content: '';
        position: absolute;
        left: -10px;
        bottom: 5px; 
        width: calc(100% + 20px);
        height: 15px;
        background-color: #EB82BE;
        opacity: 0.4; 
        z-index: -1; 
        transform: rotate(-1deg); 
        border-radius: 20px 5px 20px 5px; 
    }

    .contact-text {
        text-align: center;
        font-size: 1.1rem;
        color: #666;
        margin-bottom: 3rem;
        font-weight: 450;
    }

    .form-wrapper {
        display:flex;
        justify-content: center;
        width: 100%;
        margin: 0  auto;
    }

    .contact-form {
        width: 100%;
        background: #fdfdfd;
        padding: 2rem 3rem 3rem 3rem;
        box-shadow: 0 10px 30px rgba(0,0,0,0.05);
        border: 1px solid #eee;
        max-width: 600px;
        border-radius: 12px;
    }

    .form-group {
        margin-bottom: 1.5rem;
        text-align: left;
    }

    label {
        display: block;
        margin-bottom: 0.5rem;
        font-weight: bold;
        color: #444;
        font-size: 0.9rem;
    }

    input, textarea {
        width: 100%;
        padding: 0.8rem 1rem;
        border: 2px solid #eee;
        font-family: inherit;
        font-size: 1rem;
        transition: all 0.3s ease;
        background: #fafafa;
        box-sizing: border-box;
    }

    input:focus, textarea:focus {
        outline: none;
        border-color: #eb82be;
        background: white;
        box-shadow: 0 0 0 4px rgba(235, 130, 190, 0.1);
    }

    .submit-btn {
        width: 100%;
        padding: 1rem;
        background: #333;
        color: white;
        border: none;
        border-radius: 10px;
        font-size: 1.1rem;
        cursor: pointer;
        font-weight: bold;
        transition: transform 0.2s, background 0.2s;
        margin-top: 0.5rem;
    }

    .submit-btn:hover {
        background: #eb82be;
        transform: translateY(-2px);
    }



    @media (max-width: 1025px) {
        

        .contact-form {
            width: 100%;
            padding: 1.5rem;
        }


    }
    </style>